@model PROYECTO_SWI.Models.SolicitudMedicina

@{
    ViewData["Title"] = "Detalle de Solicitud de la Medicina";
}

<div class="container mt-4">
    <h2 class="text-primary text-center">Detalles de la Solicitud de Medicina</h2>
    <hr />
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card border-primary mb-3">
                <div class="card-header bg-primary text-white">
                    <h4 class="card-title">Información de la Solicitud</h4>
                </div>
                <div class="card-body">
                    <dl class="row">
                        <dt class="col-sm-4 font-weight-bold">Descripción:</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.Medicina.Descripcion)</dd>

                        <dt class="col-sm-4 font-weight-bold">Cantidad:</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.CantidadSolicitada)</dd>

                        <dt class="col-sm-4 font-weight-bold">Fecha:</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.Fecha)</dd>
                    </dl>
                </div>
            </div>
        </div>
    </div>
    <div class="text-center mt-4">
        <a asp-action="Edit" asp-route-id="@Model?.IdSolicitud" class="btn btn-warning btn-lg shadow-sm">Editar</a>
        <a asp-action="Index" class="btn btn-secondary btn-lg shadow-sm">Volver a la lista</a>
    </div>
</div>
